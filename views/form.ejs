<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

</head>
<body >
    <header>
        <h1><%= title %></h1>
        <h4 class="mt-3"><%= subtitle %></h4>
    </header>

    <div class="container">
        <% if (errorMsg) { %>
            <div class="error-message"><%= errorMsg %></div>
        <% } %>
        <form method="post" action="/new" class="d-flex flex-column gap-1">
            <div class="d-flex">
                <label for="name">
                    Name: 
                </label>
                <input type="text" placeholder="John Doe" name="name" id="name" required>
            </div>
            <div class="d-flex">
                <label for="message">
                    Message: 
                </label>
                <textarea name="message" id="message" cols="50" rows="4" required></textarea>
            </div>
            <div class="mt-3">
                <button type="submit" class="btn">Add message</button>
                <a href="/" class="btn btn-back">Back</a>
            </div>
        </form>
    </div>

</body>
</html>